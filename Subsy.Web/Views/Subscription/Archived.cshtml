@model List<SubscriptionsViewModel>
@using System.Globalization
@using Subsy.Domain.Enums

@{
    ViewData["Title"] = "Sonlandırılanlar";
    var tr = new CultureInfo("tr-TR");
    string[] avatarColors = { "#4f46e5","#0891b2","#059669","#d97706","#dc2626","#7c3aed","#0d9488","#db2777" };
    string AvatarColor(string name) => avatarColors[char.ToUpper(name[0]) % avatarColors.Length];

    var categoryLabels = new Dictionary<SubscriptionCategory, string>
    {
        { SubscriptionCategory.Other, "Diğer" },
        { SubscriptionCategory.Entertainment, "Eğlence" },
        { SubscriptionCategory.Music, "Müzik" },
        { SubscriptionCategory.Software, "Yazılım" },
        { SubscriptionCategory.Gaming, "Oyun" },
        { SubscriptionCategory.Cloud, "Bulut" },
        { SubscriptionCategory.Education, "Eğitim" },
        { SubscriptionCategory.News, "Haber" },
        { SubscriptionCategory.Health, "Sağlık" },
        { SubscriptionCategory.Shopping, "Alışveriş" }
    };

    var categoryChipClass = new Dictionary<SubscriptionCategory, string>
    {
        { SubscriptionCategory.Other, "chip-other" },
        { SubscriptionCategory.Entertainment, "chip-entertainment" },
        { SubscriptionCategory.Music, "chip-music" },
        { SubscriptionCategory.Software, "chip-software" },
        { SubscriptionCategory.Gaming, "chip-gaming" },
        { SubscriptionCategory.Cloud, "chip-cloud" },
        { SubscriptionCategory.Education, "chip-education" },
        { SubscriptionCategory.News, "chip-news" },
        { SubscriptionCategory.Health, "chip-health" },
        { SubscriptionCategory.Shopping, "chip-shopping" }
    };
}

<div style="display:flex; align-items:center; justify-content:space-between; margin-bottom:20px;">
    <div>
        <div class="section-title">Sonlandırılan Abonelikler</div>
        <div class="section-sub">@Model.Count abonelik</div>
    </div>
</div>

@if (!Model.Any())
{
    <div class="card">
        <div class="empty">
            <div class="empty-icon">
                <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                    <path d="M20 7H4a2 2 0 0 0-2 2v1a2 2 0 0 0 2 2h16a2 2 0 0 0 2-2V9a2 2 0 0 0-2-2z"/><path d="M10 13v4M14 13v4M8 7V5a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2"/>
                </svg>
            </div>
            <div class="empty-text">Hiç sonlandırılmış aboneliğiniz bulunmamaktadır.</div>
        </div>
    </div>
}
else
{
    <div class="sub-grid">
        @foreach (var sub in Model)
        {
            var color = AvatarColor(sub.Name);
            var periodLabel = sub.RenewalPeriodDays switch { 7 => "Haftalık", 30 => "Aylık", 365 => "Yıllık", _ => $"{sub.RenewalPeriodDays}g" };
            var hasFavicon = !string.IsNullOrWhiteSpace(sub.WebsiteUrl);
            var faviconUrl = hasFavicon ? $"https://www.google.com/s2/favicons?domain={new Uri(sub.WebsiteUrl!).Host}&sz=64" : "" ;

            <div class="sub-card">
                <div style="display:flex; align-items:flex-start; gap:10px; justify-content:space-between;">
                    <div style="display:flex; align-items:center; gap:10px;">
                        @if (hasFavicon)
                        {
                            <img src="@faviconUrl" alt="@sub.Name" class="sub-favicon" style="opacity:0.6;" onerror="this.style.display='none';this.nextElementSibling.style.display='flex';" />
                            <div class="sub-avatar" style="background:@color; opacity:0.6; display:none;">@sub.Name[0].ToString().ToUpper()</div>
                        }
                        else
                        {
                            <div class="sub-avatar" style="background:@color; opacity:0.6;">@sub.Name[0].ToString().ToUpper()</div>
                        }
                        <div>
                            <div class="sub-name" style="color:var(--text-secondary);">@sub.Name</div>
                            <div class="sub-meta">@sub.Currency · @periodLabel</div>
                        </div>
                    </div>
                    <div style="display:flex; flex-direction:column; align-items:flex-end; gap:4px;">
                        <span class="chip chip-gray">Arşiv</span>
                        <span class="chip chip-cat @categoryChipClass[sub.Category]">@categoryLabels[sub.Category]</span>
                    </div>
                </div>

                <div>
                    <div class="sub-price" style="color:var(--text-secondary);">@sub.Price.ToString("C2", tr)</div>
                    <div class="sub-meta" style="margin-top:2px;">Yenileme: @sub.RenewalDate.ToString("d MMM yyyy", tr)</div>
                </div>

                <div style="display:flex; gap:6px; flex-wrap:wrap; margin-top:auto;">
                    <form asp-action="Unarchive" asp-route-id="@sub.Id" method="post"
                          data-confirm="@sub.Name aboneliğini tekrar aktif etmek istiyor musunuz?"
                          data-confirm-title="Aktifleştirme Onayı"
                          data-confirm-confirm-text="Evet, aktif et"
                          data-confirm-variant="primary">
                        <button type="submit" class="btn btn-ghost btn-sm">Geri Al</button>
                    </form>
                    <form asp-action="Delete" asp-route-id="@sub.Id" method="post"
                          data-confirm="Bu işlem geri alınamaz. @sub.Name aboneliğini kalıcı olarak silmek istiyor musunuz?"
                          data-confirm-title="Kalıcı Silme Onayı"
                          data-confirm-confirm-text="Evet, kalıcı sil"
                          data-confirm-variant="danger">
                        <button type="submit" class="btn btn-danger btn-sm">Sil</button>
                    </form>
                </div>
            </div>
        }
    </div>
}
